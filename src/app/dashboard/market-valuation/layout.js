export const metadata = {
  title: 'Market Valuation | Vinca Wealth',
  description: 'Educational overview of market valuation context',
};

export default function MarketValuationLayout({ children }) {
  return <>{children}</>;
}
